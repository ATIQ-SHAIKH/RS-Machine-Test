"use strict";

module.exports = {
    GET_SUGGESTED_DAY_PLAN: "/get/suggested/day/plan",
    UPDATE_ACTIVITY_STATUS: "/update/activity/status",
    GET_SUGGESTED_DAY_PLAN_PENDING_COUNT: "/get/suggested/day/plan/pending/count",
    // GET /programs/{day} → Fetch activities for a given day (Day 14-21).
    // POST /activities/{activity_id}/complete → Mark an activity as complete.
    // GET /progress/{user_id} → Fetch progress of a user.
}